describe("SeatReservationService", () => {
  describe("reserveSeat", () => {
    it("should throw a type error on invalid accountID", () => {});
    it("should throw a type error on invalid totalSeatsToAllocate", () => {});
  });
});
